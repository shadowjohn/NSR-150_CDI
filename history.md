# NSR-150_CDI 歷程與研究筆記

## 2026-08-21：V0.4 靜態盤點

### 盤點範圍

- 分支：`main`，工作樹乾淨。
- 讀取 `README.md`、兩份 `.ino` 韌體、Fritzing V0.4 接線圖與既有實作紀錄。
- 初始盤點只做原始碼與電路圖的靜態分析；後續 V0.5 已完成主機端測試與 ESP8266 編譯，但仍未連接實車、量測波形或燒錄。

### 架構確認

- 控制器為 NodeMCU V2（ESP8266）。
- V0.4 接腳：D1 讀取凸台脈衝、D4 輸出 RC 訊號、D6 經 2SC1815 觸發 SCR 點火。
- 脈衝輸入以 PC817 隔離；高壓儲能／點火端使用 BT151-500R、1N5408 與兩顆高耐壓麥拉電容；控制端以二極體降壓後的 7805 供電。
- V0.4 在開機時先輸出 250 次、每次 HIGH/LOW 各 3 ms 的 RC 訊號，再依轉速決定是否繼續輸出 RC 訊號。

### 已確認的程式問題

#### P0：15,000 rpm 以上的點火表插值會除以零

`countup()` 先將 `index` 與 `index_n` 限制在 `0..15`，但在 15,000 rpm 以上兩者都會變成 `15`：

```text
index = 15
index_n = 15
s = 15000
e = 15000
map(rpm, s, e, ss, ee)
```

因此 `map()` 的來源區間為 `15000..15000`，除數為零。程式雖然容許至 17,000 rpm，實際可正常插值的上限只有 14,999 rpm。

處理此問題前，不應在車上測試 15,000 rpm 以上。

### 時序與可維護性觀察

- ISR 取得凸台時戳並算出 `CDI_DELAY`，真正的 `delayMicroseconds()` 卻在 `loop()` 才執行；從 ISR 回到主迴圈的延遲會直接變成額外點火延遲。
- 正式點火流程旁仍保留高頻 `Serial.print()` 偵錯輸出。這會增加主迴圈延遲與點火抖動，應在實車版關閉或改為非同步診斷。
- `isFiring` 是單一旗標而非事件佇列，且 `rpm`、`RPM_DELAY`、`CDI_DELAY` 都由 ISR 與主迴圈共用；後續若重構，應以同一次 ISR 的不可分割快照來排程點火。
- `RPM_DELAY > 598802` 已把低於約 100 rpm 視為停止，後續的 `rpm < 80` 判斷在現有流程中沒有實質作用；最低有效轉速規格應統一。
- V0.4 實作文章說 RC 於 5,500 rpm 以上開啟，現行韌體則使用 `rpm >= 5000`。應確認 5,000 或 5,500 rpm 才是正式設定。

### 建議驗證順序

1. 修正 15,000 rpm 以上的查表邏輯：明確採用最後一格飽和值，或將限轉值降到表格可涵蓋的範圍。
2. 移除正式點火路徑中的 Serial 偵錯，並使排程以 ISR 取得的原始時戳為基準。
3. 先用 555 或訊號產生器與示波器，在低壓邏輯側量測 D1、D6、D4；至少測 1k、5k、10k、14,999 與 15k rpm。
4. 只有在波形、延遲與限轉行為都符合預期後，才接回高壓點火端與實車測試。

### 參考位置

- `README.md`
- `CODE/NSR-150_CDI_V0.4/NSR-150_CDI_V0.4.ino`
- `fritzing/NSR_CDI_V0.4.fzz`
- `https://3wa.tw/blog/blog.php?id=1931`
- `https://3wa.tw/blog/blog.php?id=1934`

## 2026-08-21：V0.5 保守時序調整

### 調整範圍

- 新增 `CODE/NSR-150_CDI_V0.5/NSR-150_CDI_V0.5.ino`，不修改 V0.1~V0.4。
- 保留 V0.4 的點火角表、RC 開啟門檻（5,000 rpm）、200 us SCR 觸發脈寬與高壓回路。
- 不因高壓線圈曾燒毀而猜測性調整電容、SCR、點火角或觸發脈寬。

### 已完成調整

- 點火表改為整數千分之一度計算；0~14,999 rpm 維持線性插值，15,000~17,000 rpm 固定使用最後一格 8°，不會再呼叫零長度的 `map()` 區間。
- 有效轉速統一為 100~17,000 rpm，超出範圍或過慢脈衝都回到不點火狀態。
- 點火表的升角與降角區間都使用有號插值，保留 V0.4 在 4k~5k、5k~6k 等下降區間的既有角度曲線。
- `micros()` 量化後的 3,529 us 脈衝視為 17,000 rpm 上限，避免實際 17,000 rpm 因整數除法算成 17,002 rpm 而漏點火；3,528 us 以下仍拒絕。
- 移除正式點火流程的 Serial 與主迴圈 `delayMicroseconds()`；D1 ISR 以凸台原始時戳扣除運算耗時後，使用 ESP8266 Timer1 one-shot 安排點火。
- Timer1 第一個事件拉高 D6，第二個事件於 200 us 後拉低 D6；每個凸台脈衝只保留一個待執行點火事件，未結束的舊事件會先被安全關閉。
- RC 訊號仍在 5,000 rpm 以上輸出，並在這次點火結束時關閉。

### 驗證結果

- 新增 `tests/v0.5_timing_test.cpp`，已先確認缺少 V0.5 計時模組時編譯失敗，再完成實作後通過。
- 主機端測試通過：100/17,000 rpm 邊界、3,528/3,529/3,530 us 脈衝量化、2,500 rpm 升角插值、4,500 rpm 降角插值、14,999/15,000/17,000 rpm 的最後一格飽和值、4,999/5,000 rpm RC 門檻與 200 us 脈寬常數。
- NodeMCU V2、ESP8266 Arduino Core 3.1.2 編譯通過：V0.5 RAM 28,332 / 80,192 bytes（35%）、IRAM 60,255 / 65,536 bytes（91%）、Flash 232,180 / 1,048,576 bytes（22%）。
- 同環境 V0.4 編譯結果為 IRAM 61,211 / 65,536 bytes（93%）；V0.5 沒有增加 IRAM 壓力。

### 未驗證範圍與上車前檢查

- 尚未量測 D1→D6 延遲抖動、D6 實際脈寬、D4 RC 波形，亦未燒錄或實車測試。
- 上車前先用 555 或訊號產生器，在低壓邏輯側以示波器測試 1k、5k、10k、14,999、15k、17k rpm。
- 確認無重複點火、無重開機、D6 為 200 us、D4 在 5k rpm 門檻正確切換後，才進行原地正時燈與短距離低負載測試。
